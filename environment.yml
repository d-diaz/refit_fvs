name: refit_fvs

channels:
  - conda-forge

dependencies:
  - arviz
  - nvidia::cuda-nvcc
  # - earthengine-api
  - geopandas
  - ipywidgets
  - jax
  - jinja2
  - jupyterlab
  - matplotlib
  - pandas
  - pip
  - pip:
      - numpyro[cuda] -f https://storage.googleapis.com/jax-releases/jax_cuda_releases.html
      - -e .  # install local refit_fvs package in editable mode
  - psycopg2
  - pvlib-python
  - python
  - scikit-learn
  - seaborn
  - sqlalchemy
  - tqdm
  - xlrd

prefix: /users/ddiaz/miniconda3/refit_fvs
